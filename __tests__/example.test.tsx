import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';

describe('Basic Application Test', () => {
  it('renders a generic test passing', () => {
    expect(true).toBe(true);
  });
});
